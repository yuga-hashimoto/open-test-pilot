{
  "name": "@open-test-pilot/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit --pretty false",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "build": "tsc --build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@open-test-pilot/github-adapter": "workspace:*",
    "@open-test-pilot/runner-protocol": "workspace:*",
    "@open-test-pilot/scheduler": "workspace:*",
    "@types/pg": "^8.20.0",
    "fastify": "^5.6.1",
    "pg": "^8.22.0"
  }
}
