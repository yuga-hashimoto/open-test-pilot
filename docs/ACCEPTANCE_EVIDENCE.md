# Acceptance evidence (2026-07-17)

This file records evidence that was executed, not only code that exists.

| Scenario | Evidence | Result |
| --- | --- | --- |
| Personal Web | `pnpm testpilot run examples/manifests/complex-flow.yaml --actions examples/custom-actions.mjs` produced `run-1784243071149-iwqizq`; report passed with 5 steps and 9 artifacts. The live browser then loaded the team API, edited/saved the Manifest, opened Generated TS and Graph, and selected a passed server run. | Core Web/mixed flow passed. |
| Failure → Codex repair → rerun | Intentionally broken `examples/manifests/repair-login.yaml` produced local failed run `run-1784247101958-1ldk4f` with `LOCATOR_CHANGED` evidence. `CodexCodeWorker` request `repair-live-2` returned a correlated structured AgentResult and a Manifest-only proposal changing `role=button[name=Sign in]` to `role=button[name=ログイン]`. The proposed Manifest validated and reran as local passed run `run-1784247382599-ctpnx1` with 4/4 actions passed and 5 artifacts. | Passed locally with real Codex and Chromium. |
| Source-first | `pnpm testpilot source analyze examples/fixtures/web/server.mjs --framework nextjs --output /tmp/opentestpilot-generated.yaml --base-url http://127.0.0.1:4173` produced 3 findings; `pnpm testpilot manifest validate /tmp/opentestpilot-generated.yaml` returned `valid`. | Passed for the implemented analyzer/generator path. |
| Team API/results | Run `run-eb6ac2d4-93c9-4933-921b-d05b8bb72fae` was created, runner registered, job leased, 11 physical artifacts uploaded, job completed `passed`, and report bytes were fetched and SHA256 checked. The Codex repair acceptance also uploaded 11 failure artifacts to server run `run-beb64d45-cbca-400e-ba10-d395d5bd2079` (`failed`, 1 failure) and 5 repaired artifacts to `run-db5cebfe-7a9e-4b6b-867c-974df477f04e` (`passed`, 0 failures); the browser Evidence view opened the live artifact blobs. | Passed on the live in-memory server. |
| Fresh Codex end-to-end run | Codex created org `org-1d1a4802-b01e-4aa5-9478-d829658fdb2c`, project/test/run, registered runner `runner-02755d32-2aa1-4e27-b381-3470c6d282a5`, leased `job-run-6d9a227d-cec3-42e0-8526-170568a7a1e9`, executed the real fixture flow, uploaded 6 artifacts, and completed `passed` with 0 failures. `pnpm testpilot manifest validate` and `pnpm testpilot run` also passed. | Passed. |
| External services | PostgreSQL/Redis/MinIO-backed server created org `814d05eb-a781-4f49-9dca-271cc893673f`, leased `job-7c846bea-2a6d-44dc-b68b-bd88f6ca97bc`, uploaded/fetched an S3 artifact, and completed run `7c846bea-2a6d-44dc-b68b-bd88f6ca97bc` as passed. | Passed with real local containers. |
| Remote Docker Runner | Fresh `docker build` passed for both runner/server images. Latest `test:docker:smoke` created `run-29dd69fd-a2e9-4181-b6a1-94ec1c4c3e3a`; the rebuilt container reached the host fixture via `host.docker.internal`, ran Chromium, uploaded 8 artifacts, and completed with 0 failures. | Passed. |
| Runner UI | Added/listed runner API was covered by server/queue tests. A real browser loaded the rebuilt web app against the restarted server and displayed `live-chromium`, Chromium capability, max concurrency 1, and `heartbeat Now` with zero console errors. | Passed. |
| Team/GitHub | Existing Chrome tab showed `OpenTestPilot E2E` installed for `yuga-hashimoto/open-test-pilot`, installation `146977164`, with the required write permissions. The real ignored App key was loaded without printing it; the App JWT → installation-token flow then passed server sync, branch listing/creation, commit, read-back, compare, Draft PR creation, Check Run, Commit Status, PR history, and cleanup. Temporary Draft PRs `#2` and `#3` were closed and their branches deleted. | Real GitHub App repository workflow passed. OAuth client setup/login remains a separate external gate. |
| GitHub integration UI/API gate | A fresh server loaded repository `yuga-hashimoto/open-test-pilot` with installation `146977164` into the live Web GitHub view. The browser created tenant change request `change-request-02e975a6-92a9-4139-8a9c-f47a4d7d5313`. The current server routes were then run with the real App credentials: sync, branches, contents, compare, branch creation, commit, Draft PR creation, and PR history all returned successful live responses. A final headless Chromium pass verified the rendered `tests/login.yaml · file-sha` source row and `#12 · Closed repair` history row with zero console errors. | Tenant-safe UI/API and real App workflow passed; OAuth client/session login remains external. |
| GitHub read integration | Using the real installation token (token not printed), the adapter read repository metadata, decoded `examples/manifests/fixture-login.yaml` at `main` with SHA `7137a60e9e40ea1360a7301905963dd3ed3a6846`, and listed all PRs. Server routes expose the same content and open/closed/all history through the tenant-scoped GitHub App path, and Web GitHub view renders both surfaces. | Passed against real GitHub App read APIs. |
| Complex DSL | Full test suite includes generator, schema, adapter, custom action, branch, loop, parallel, retry, try/finally, and result-import coverage. | Passed in automated tests; the complex fixture also passed as a real local run. |
| AI Worker/Codex | Structured parser and CLI worker tests pass. The live in-memory API completed `aijob-a616efce-710e-47e4-8650-436fc8b74303` through create → lease → real `CodexCodeWorker` analyze-failure → complete, persisted the structured result, and recorded audit events. The PostgreSQL-backed API also completed job `9c36f9d9-4fe2-4491-9219-6b7a5f0d9785` after migration `010_ai_worker_jobs.sql`. | Analyze-failure → Codex result → job completion passed on both in-memory and PostgreSQL persistence. PR publication remains gated on live GitHub App credentials. |
| GitHub branch/compare | Added tenant-safe GitHub App branch listing and branch comparison routes plus the live Web comparison panel. The real App smoke created a temporary branch, compared it against `main` with `aheadBy: 1`, and deleted it after verification. | Code/API/UI and live App comparison passed. |
| Custom Action safety | Playwright adapter tests cover declared network/filesystem/secret permissions, deny execution when Manifest permissions are absent, and redact a resolved secret from failed run evidence. | Passed. |
| AI Worker full local workflow | Request `codex-real-1784279967195` used the real Codex CLI and a real Git clone/fetch workspace, applied the returned Manifest proposal, validated it with the repository parser, and reran the real Chromium fixture as `run-1784280036815-bqz59z` with 3 artifacts and no failures. GitHub publication was separately verified through the live App server/adapter smoke with temporary PR creation and cleanup. | Repair → apply → validate → rerun and App publication boundary passed. |
| Schedules/UI evidence | Live server schedule `schedule-552b09a8-40c9-4bf7-99c5-6a5c3716e003` returned `202` from the API trigger and the rebuilt browser displayed `Run now →`; clicking it created queued run `run-8003cffc-647f-433c-87e0-7ac0ebac85dd` and navigated to the live Runs view. | Passed for API/UI trigger path; cron daemon deployment remains operational gate. |
| Tenant settings/secrets | Live browser opened Settings against the fresh server, created `browser-token`, showed the masked reference and `Secret metadata saved`, and the body did not contain `browser-secret-value`; direct API metadata also omitted the value. | Passed for write-only UI and API contract. |
| Cancellation/concurrency | Scheduler/queue/server tests cover max concurrency, tenant-safe job cancellation, and cancelled run state. | Passed in automated tests. |
| Scheduler daemon | `apps/scheduler` was built and its Docker image `opentestpilot-scheduler:local` was built successfully. Against the live in-memory server, schedule `schedule-d927a136-64a7-4648-b465-494a8b1e90a8` (`* * * * *`) produced queued run `run-11bdee7b-d660-4e14-b34c-aae0bb12fd33`; the second poll returned `[]`, proving per-minute deduplication and tenant headers. | Passed for the daemon poll/trigger path; production uptime/monitoring remains deployment-specific. |
| Android/iOS | Android `emulator-5554` was started locally (Android 16/API 36); Appium 3.5.2 + UiAutomator2 8.1.0 ran the direct assertion and the Android Settings Manifest successfully, including the `Network & internet` text assertion and real 1080x2400 screenshots. For iOS, Appium 3.5.2 + XCUITest 11.17.7 selected a real iOS 26.4 simulator and `xcodebuild` completed `TEST BUILD SUCCEEDED`, but the WebDriverAgent XCTest runner never exposed its HTTP server (8101) and the adapter timed out at session creation. | Android acceptance path passed. iOS is an explicitly verified host-environment gate: WDA startup timed out after a successful build; no iOS pass is claimed. |

## Repeatable gates

```sh
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm verify:generated
pnpm license:report
pnpm release:artifacts
pnpm security:audit
```
