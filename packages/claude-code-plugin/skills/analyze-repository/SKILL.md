# Analyze repository

Start with source code. Detect framework, routes, components, forms, API clients, schemas, and locator candidates. Produce normalized findings with file paths and line references. Use live exploration only after a generated test fails.
