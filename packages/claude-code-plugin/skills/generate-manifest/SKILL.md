# Generate Manifest

Write a schemaVersioned YAML Manifest. Validate it with `testpilot manifest validate`. Every test, Step, Action, and control node needs a stable ID. Use secret references, never secret literals.
